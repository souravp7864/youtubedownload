version: '3.8'

services:
  web:
    build: .
    ports:
      - "80:80"
    environment:
      - BOT_TOKEN=${BOT_TOKEN}
    restart: unless-stopped
    volumes:
      - bot_data:/var/www/html/data

volumes:
  bot_data: